export const PACKAGE_NAME = "@orchestra/core";

/**
 * Scaffold placeholder. Replaced by the state machine transition table and
 * domain types (design.md §5, build order step 1). Exists so the package
 * builds, is importable by its dependents, and has something to test.
 */
export function placeholder(): string {
  return PACKAGE_NAME;
}
